backend:
  build: .
  ports:
   - "8080:8080"
  volumes:
   - ./data:/data
  links:
   - pg
   - logstash
pg:
  image: postgres:latest
logstash:
  build: ./logstash
  ports:
   - "9292:9292"
   - "9200:9200"
  volumes:
   - ./logstash/conf.d:/opt/logstash/conf.d/ 
